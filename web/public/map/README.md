# 地图运行时资源

本目录只存放前端代码实际引用的轻量成品，不保存源图、候选图、审核截图或生成过程文件。

- 正式运行时格式优先使用 WebP。
- 源 PNG、Prompt、模型信息和回传记录存放在项目根目录的 `map-art/`。
- 新资源接入后要同步更新代码引用；下线资源应从本目录移除，避免“可能有用”的副本长期堆积。
- `world/` 当前只包含世界页常驻母版和三张同机位 focus 变体。
