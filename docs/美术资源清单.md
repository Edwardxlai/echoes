# 回响美术资源清单

> 最近整理：2026-07-13。以实际文件、代码引用和 SHA-256 检查为准。

## 1. 三类目录

| 位置 | 性质 | 能否被前端直接引用 |
|---|---|---|
| `map-art/` | 源图、参考图、候选图、共享模块和生产记录 | 否 |
| `web/public/` | 运行时成品；只保留代码实际引用的资源 | 是 |
| `prototype/public/` | 早期原型归档，仍由 prototype HTML 使用或作为版式参考 | 仅原型 |

`.preview/` 与 `.preview-shots/` 是可再生成的浏览器缓存和验收截图，不是美术资产，已清理并由 `.gitignore` 排除。

## 2. 当前运行时资源

| 分组 | 数量 | 状态 |
|---|---:|---|
| 世界地图 | 4 WebP | 1 张常驻母版 + 3 张同机位 focus 变体，全部有代码引用 |
| 品牌标记 | 1 PNG | 192×192 透明图，页面显示尺寸 34–40px |
| 视频封面 | 9 SVG | `data.ts` 中 v1–v9 逐一引用 |
| favicon | 1 ICO | Next.js 应用图标 |

世界地图四张运行图从 PNG 的 9,621,324 字节降至 WebP 的 777,350 字节，减少约 91.9%。品牌标记从 1,254×1,254 / 319,470 字节降至 192×192 / 8,844 字节。

## 3. 已清理的冗余

- 删除 4 张无代码引用的世界层旧 PNG：`world-sea` 与 economy / history / technology region，合计约 7.45 MB。
- 删除 `web/public/map/world/atmosphere/` 的 4 个未接入副本；相应候选只保留在 `map-art/60_common/`。
- 删除 Next.js 初始模板自带且未引用的 5 个 SVG。
- 清理 `.preview/` 与 `.preview-shots/` 的重复截图、临时抠图结果和浏览器扩展缓存。
- 精确哈希检查发现的唯一跨目录重复文件是 water detail WebP；运行时副本未被引用，现只保留源工程版本。

## 4. 源工程状态

- `00_style/reference-t00/`：10 张 T00 参考图，全部只读；其中需要透明的图实际为 RGB，不可直接上线。
- `20_world/source/`：W01、W02、W05 三张世界层源图。
- `30_regions/economy/source/`：W04 一张经济区域页候选。
- `60_common/clouds/source/`：W06–W08 三张云源图；另有 3 张已处理透明 PNG。
- `60_common/water/`：1 张水面细节 WebP 候选。
- 原批次 W03 文件缺失；2026-07-14 已补充 `W03_world_connected_terrain_alpha_v01.png` 作为视觉与建模参考，但重构方案要求的正式 GLB/LOD 仍未生产。

## 5. 后续入库检查

1. 新生成图先进入 `map-art`，不得直接丢进 `public`。
2. 确认页面真实引用后，再导出 WebP 到对应运行时目录。
3. 接入前检查尺寸、真实 alpha、命名、文件体积和移动端表现。
4. 下线代码引用时同步删除运行时文件；源图是否归档由回传记录决定。
